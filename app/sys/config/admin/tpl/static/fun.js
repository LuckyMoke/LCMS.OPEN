if($('.plugin-test-btn').length>0){$('.plugin-test-btn').on('click',function(){var url=$(this).attr('data-url');layer.prompt({title:'请输入收件地址',formType:0},function(email,index){layer.close(index);LJS._post(url,{email:email},function(res){if(res.code=='1'){LJS._tips(res.msg)}else{LJS._tips(res.msg,0)}})})})};